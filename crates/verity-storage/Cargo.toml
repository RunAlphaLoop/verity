[package]
name = "verity-storage"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
verity-core = { path = "../verity-core" }
moka = { workspace = true }
sqlx = { workspace = true }
tokio = { workspace = true }
pgvector = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
async-trait = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true }
# Envelope encryption for L0 payloads + DEK wrapping (SPEC §8a, crypto.rs).
aes-gcm = "0.10"
rand_core = { workspace = true }
# Subject hashing for the surviving erasure audit row (SPEC §8b).
sha2 = { workspace = true }
# Salted-HMAC fingerprint of connector credentials (VERITY_SCOPE_KEY),
# Phase-2 secret intake — never a bare hash of the plaintext.
hmac = { workspace = true }

[dev-dependencies]
rand = { workspace = true }
