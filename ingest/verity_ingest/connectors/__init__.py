"""Native flagship connectors (SPEC.md §5, tier 1).

Each module implements the Connector ABC from ``verity_ingest.connector``.
Flagships are few by design: the sources where the product's claims live.
"""
